---
status: in_progress
quick_id: 260515-u32
created: 2026-05-15
---

# Quick Task 260515-u32: Fix run-telldus CONFIG_PATH environment override

## Goal

Make `CONFIG_PATH=/path/to/tellstick.conf ./scripts/run-telldus.sh` behave as
documented in QUICKSTART.

## Tasks

1. Update `scripts/run-telldus.sh` so `CONFIG_PATH` from the environment takes
   precedence over the placeholder default.
2. Adjust script comments and error text so users are not told they must edit
   the script when using the environment variable.
3. Verify the script reports the caller-provided path when the path is invalid.
