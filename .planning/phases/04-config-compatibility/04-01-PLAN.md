---
phase: 04-config-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - telldus-core/service/CMakeLists.txt
  - telldus-core/service/SettingsConfuse.cpp
  - telldus-core/service/main_unix.cpp
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "Default state install directory is /var/lib/telldus instead of /var/state"
    - "Runtime env vars TELLDUS_CONFIG_FILE and TELLDUS_STATE_DIR override compile-time paths"
    - "Daemon auto-creates state directory on startup if missing"
  artifacts:
    - path: "telldus-core/service/CMakeLists.txt"
      provides: "Default STATE_INSTALL_DIR updated to /var/lib/telldus"
      contains: "DEFAULT_STATE_INSTALL_DIR \"/var/lib/telldus\""
    - path: "telldus-core/service/SettingsConfuse.cpp"
      provides: "Runtime path resolution with getenv fallback"
      contains: "TELLDUS_CONFIG_FILE|TELLDUS_STATE_DIR"
    - path: "telldus-core/service/main_unix.cpp"
      provides: "State directory auto-creation at daemon startup"
      contains: "mkdir.*0755"
  key_links:
    - from: "telldus-core/service/CMakeLists.txt"
      to: "telldus-core/service/config.h"
      via: "CONFIGURE_FILE"
      pattern: "STATE_INSTALL_DIR.*CACHE PATH"
    - from: "telldus-core/service/SettingsConfuse.cpp"
      to: "telldus-core/service/config.h"
      via: "CONFIG_FILE|VAR_CONFIG_FILE"
      pattern: "getenv.*TELLDUS"
    - from: "telldus-core/service/main_unix.cpp"
      to: "/var/lib/telldus"
      via: "mkdir"
      pattern: "ensureStateDirectory|mkdir.*state"
---

<objective>
Modernize Linux config and state file paths so the daemon works on current Linux distributions and supports runtime overrides for Docker testing.

Purpose: Replace the deprecated `/var/state` default, add environment variable path overrides, and ensure the state directory exists on startup.
Output: Modified `CMakeLists.txt`, `SettingsConfuse.cpp`, and `main_unix.cpp` with zero new compiler warnings.
</objective>

<execution_context>
@/home/rsw/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rsw/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-config-compatibility/04-CONTEXT.md
@.planning/phases/04-config-compatibility/04-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
From telldus-core/service/CMakeLists.txt:
- DEFAULT_STATE_INSTALL_DIR = "/var/state" (Linux, not FreeBSD)
- STATE_INSTALL_DIR cache path
- SYSCONF_INSTALL_DIR = "/etc"

From telldus-core/service/config.h.in:
- CONFIG_PATH = "@SYSCONF_INSTALL_DIR@"
- VAR_CONFIG_PATH = "@STATE_INSTALL_DIR@"

From telldus-core/service/SettingsConfuse.cpp:
- const char* CONFIG_FILE = CONFIG_PATH "/tellstick.conf"
- const char* VAR_CONFIG_FILE = VAR_CONFIG_PATH "/telldus-core.conf"
- readConfig() uses CONFIG_FILE
- readVarConfig() uses VAR_CONFIG_FILE
- All write methods use CONFIG_FILE or VAR_CONFIG_FILE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update default state install directory in CMakeLists.txt</name>
  <files>telldus-core/service/CMakeLists.txt</files>
  <read_first>
    - telldus-core/service/CMakeLists.txt (lines 99-109 for Linux state path defaults)
    - .planning/phases/04-config-compatibility/04-RESEARCH.md (Pitfall 1: /var/state missing)
    - .planning/phases/04-config-compatibility/04-CONTEXT.md (D-04-01, D-04-13)
  </read_first>
  <action>
    In telldus-core/service/CMakeLists.txt, change the Linux DEFAULT_STATE_INSTALL_DIR from "/var/state" to "/var/lib/telldus".
    The FreeBSD branch ("/var/spool") must remain untouched per D-04-13.
    Specifically:
    - Locate the ELSE() branch inside the IF(${CMAKE_SYSTEM_NAME} MATCHES "FreeBSD") block
    - Change SET(DEFAULT_STATE_INSTALL_DIR "/var/state") to SET(DEFAULT_STATE_INSTALL_DIR "/var/lib/telldus")
    - Do NOT modify the FreeBSD SET(DEFAULT_STATE_INSTALL_DIR "/var/spool") line
    - Do NOT modify SYSCONF_INSTALL_DIR (remains "/etc")
    - Verify the install permissions for telldus-core.conf remain: OWNER_READ OWNER_WRITE GROUP_READ GROUP_WRITE WORLD_READ WORLD_WRITE (to be addressed in 04-03 if changed)
  </action>
  <acceptance_criteria>
    - telldus-core/service/CMakeLists.txt contains DEFAULT_STATE_INSTALL_DIR "/var/lib/telldus" in the Linux (not FreeBSD) branch
    - telldus-core/service/CMakeLists.txt still contains DEFAULT_STATE_INSTALL_DIR "/var/spool" in the FreeBSD branch
    - The line SET(DEFAULT_STATE_INSTALL_DIR "/var/state") no longer exists in the file
  </acceptance_criteria>
  <verify>
    <automated>
      grep -n 'DEFAULT_STATE_INSTALL_DIR "/var/lib/telldus"' telldus-core/service/CMakeLists.txt && \
      grep -n 'DEFAULT_STATE_INSTALL_DIR "/var/spool"' telldus-core/service/CMakeLists.txt && \
      ! grep -n 'DEFAULT_STATE_INSTALL_DIR "/var/state"' telldus-core/service/CMakeLists.txt && \
      echo "PASS"
    </automated>
  </verify>
  <done>Linux default state directory changed from /var/state to /var/lib/telldus; FreeBSD path preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime path override via environment variables</name>
  <files>telldus-core/service/SettingsConfuse.cpp</files>
  <read_first>
    - telldus-core/service/SettingsConfuse.cpp (lines 29-30 for CONFIG_FILE and VAR_CONFIG_FILE constants)
    - telldus-core/service/SettingsConfuse.cpp (lines 364-453 for readConfig and readVarConfig)
    - telldus-core/service/config.h (generated, or config.h.in for constant names)
    - .planning/phases/04-config-compatibility/04-CONTEXT.md (D-04-04, D-04-05)
  </read_first>
  <action>
    Add runtime environment variable resolution to SettingsConfuse.cpp.
    Replace the global const char* CONFIG_FILE and VAR_CONFIG_FILE with helper functions that check getenv first:

    1. Add #include <unistd.h> if not already present (for getpid/getenv, though getenv is in <stdlib.h> which may already be included).
    2. Add a helper function getConfigFilePath() that:
       - Checks getenv("TELLDUS_CONFIG_FILE")
       - Returns the env value if non-NULL and non-empty
       - Falls back to CONFIG_FILE (the compile-time constant)
    3. Add a helper function getVarConfigDir() that:
       - Checks getenv("TELLDUS_STATE_DIR")
       - Returns the env value if non-NULL and non-empty
       - Falls back to VAR_CONFIG_PATH (the compile-time constant)
    4. Replace all direct uses of CONFIG_FILE in SettingsConfuse.cpp with getConfigFilePath().
    5. Replace all direct uses of VAR_CONFIG_FILE in SettingsConfuse.cpp with (getVarConfigDir() + "/telldus-core.conf").
    6. The helper functions must be declared before first use. They can be static functions or static inline functions.
    7. Do NOT use std::filesystem (C++17) since the project may not be compiled with C++17.
    8. Use std::string for path concatenation to avoid buffer issues.
    9. The existing const char* declarations at lines 29-30 should be removed or converted to the new helper pattern.
  </action>
  <acceptance_criteria>
    - SettingsConfuse.cpp contains a function or logic that checks getenv("TELLDUS_CONFIG_FILE")
    - SettingsConfuse.cpp contains a function or logic that checks getenv("TELLDUS_STATE_DIR")
    - All fopen() calls for the stable config use the resolved config path (not hardcoded CONFIG_FILE)
    - All fopen() calls for the var config use the resolved state path (not hardcoded VAR_CONFIG_FILE)
    - The compile-time constants CONFIG_FILE and VAR_CONFIG_FILE remain as fallbacks
    - No new compiler warnings are introduced (build with existing -Wall -Wextra flags)
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q 'getenv.*TELLDUS_CONFIG_FILE' telldus-core/service/SettingsConfuse.cpp && \
      grep -q 'getenv.*TELLDUS_STATE_DIR' telldus-core/service/SettingsConfuse.cpp && \
      grep -c 'fopen.*getConfigFilePath\|fopen.*getVarConfigPath\|fopen.*configPath\|fopen.*varConfigPath' telldus-core/service/SettingsConfuse.cpp | grep -q '[1-9]' && \
      echo "PASS"
    </automated>
  </verify>
  <done>Runtime env var overrides for config and state paths are implemented with compile-time fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-create state directory at daemon startup</name>
  <files>telldus-core/service/main_unix.cpp</files>
  <read_first>
    - telldus-core/service/main_unix.cpp (current startup sequence)
    - telldus-core/service/TelldusMain.cpp (TelldusMain::start() to understand when Settings is constructed)
    - .planning/phases/04-config-compatibility/04-CONTEXT.md (D-04-02, D-04-03)
    - .planning/phases/04-config-compatibility/04-RESEARCH.md (Pattern 2: state directory creation)
  </read_first>
  <action>
    Add state directory auto-creation to the Linux daemon startup path.
    In telldus-core/service/main_unix.cpp:
    1. Before calling TelldusMain::start() (or before daemonizing if daemonization happens first), resolve the state directory path:
       - Check getenv("TELLDUS_STATE_DIR")
       - Fall back to the compile-time VAR_CONFIG_PATH (which resolves to /var/lib/telldus after Task 1)
    2. Create the directory if it does not exist using mkdir() with mode 0755:
       - Call stat() to check existence
       - If missing, call mkdir(path, 0755)
       - If mkdir fails with EEXIST, that is acceptable (another process created it)
       - If mkdir fails for any other reason, log a warning using Log::warning() or fprintf(stderr, ...) if Log is not yet initialized
    3. Do NOT create the stable config directory (/etc) — that is the OS's responsibility.
    4. Do NOT change the daemon's working directory or privilege-dropping logic.
    5. Include <sys/stat.h> and <sys/types.h> and <errno.h> as needed.
  </action>
  <acceptance_criteria>
    - main_unix.cpp contains logic that resolves the state directory path (env var or compile-time fallback)
    - main_unix.cpp contains a stat()/mkdir() sequence to create the directory with 0755 permissions
    - The directory creation happens before TelldusMain::start() or before Settings construction
    - The code does NOT attempt to create /etc or the stable config directory
    - Build compiles with zero new warnings
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q 'mkdir' telldus-core/service/main_unix.cpp && \
      grep -q 'stat' telldus-core/service/main_unix.cpp && \
      grep -q 'TELLDUS_STATE_DIR\|VAR_CONFIG_PATH' telldus-core/service/main_unix.cpp && \
      echo "PASS"
    </automated>
  </verify>
  <done>Daemon startup auto-creates the state directory (/var/lib/telldus by default) with 0755 permissions.</done>
</task>

<task type="auto">
  <name>Task 4: Build and verify zero new warnings</name>
  <files>build/headless/</files>
  <read_first>
    - telldus-core/CMakeLists.txt (compiler flags: -Wall -Wextra -Werror=deprecated-declarations -Werror=return-type)
    - CMakePresets.json (headless preset)
  </read_first>
  <action>
    Build the modified code to confirm no new compiler warnings:
    1. Run cmake --preset headless (or equivalent cmake -B build/headless ...)
    2. Run cmake --build build/headless --target telldusd 2>&1 | tee build/headless/04-01-build.log
    3. Verify the log contains zero new warnings specific to SettingsConfuse.cpp or main_unix.cpp.
    4. If warnings appear, fix them before marking this task done.
  </action>
  <acceptance_criteria>
    - cmake configure succeeds
    - cmake build of telldusd target succeeds
    - Build log does not contain new warnings from SettingsConfuse.cpp or main_unix.cpp
    - build/headless/04-01-build.log exists and is non-empty
  </acceptance_criteria>
  <verify>
    <automated>
      test -f build/headless/service/telldusd && \
      test -f build/headless/04-01-build.log && \
      ! grep -i 'warning.*SettingsConfuse\|warning.*main_unix' build/headless/04-01-build.log && \
      echo "PASS"
    </automated>
  </verify>
  <done>Modified code builds successfully with the existing strict warning flags and produces no new warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon process -> filesystem | Daemon reads/writes config and state files |
| Environment -> Daemon | Env vars TELLDUS_CONFIG_FILE and TELLDUS_STATE_DIR influence file paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Env var path override | mitigate | Document that absolute paths are required; daemon runs as root in v1 so path traversal is limited by filesystem permissions |
| T-04-01 | Denial of Service | State directory creation failure | accept | Log warning and continue; v1 runs as root so creation should succeed |
| T-04-01 | Information Disclosure | World-readable state file | mitigate | Install permissions remain 644 (owner read/write, group read, world read); state contains only device on/off values, no secrets |
</threat_model>

<verification>
- DEFAULT_STATE_INSTALL_DIR is "/var/lib/telldus" in the Linux branch of service/CMakeLists.txt
- SettingsConfuse.cpp resolves config and state paths via getenv with compile-time fallback
- main_unix.cpp creates the state directory before daemon startup
- Build produces zero new warnings
</verification>

<success_criteria>
- Modern Linux distributions have a valid default state directory path
- Docker and non-standard installs can override paths without recompilation
- Daemon starts successfully even when the state directory does not yet exist
</success_criteria>

<output>
After completion, create `.planning/phases/04-config-compatibility/04-01-SUMMARY.md`
</output>
