---
phase: 04-config-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - telldus-core/service/SettingsConfuse.cpp
  - telldus-core/service/CMakeLists.txt
  - tests/integration/state-persistence-smoke.sh
autonomous: true
requirements:
  - CONF-02
  - CONF-04

must_haves:
  truths:
    - "Device state (on/off/dim) is written only to the var config file"
    - "Stable config tellstick.conf is never modified by state changes"
    - "State persists across daemon restarts when the var config file is present"
    - "Read-only stable config returns TELLSTICK_ERROR_PERMISSION_DENIED on write attempts"
  artifacts:
    - path: "tests/integration/state-persistence-smoke.sh"
      provides: "Automated test for state separation and persistence"
      contains: "tdtool --on\|tdtool --off\|restart\|telldus-core.conf"
    - path: "telldus-core/service/SettingsConfuse.cpp"
      provides: "Correct write target separation and permission error propagation"
      contains: "TELLSTICK_ERROR_PERMISSION_DENIED"
    - path: "telldus-core/service/CMakeLists.txt"
      provides: "Secure install permissions for state file"
      contains: "OWNER_READ OWNER_WRITE"
  key_links:
    - from: "tests/integration/state-persistence-smoke.sh"
      to: "telldus-core/service/SettingsConfuse.cpp"
      via: "tdtool commands trigger setDeviceState"
      pattern: "setDeviceState.*VAR_CONFIG_FILE"
    - from: "telldus-core/service/CMakeLists.txt"
      to: "/var/lib/telldus/telldus-core.conf"
      via: "INSTALL FILES"
      pattern: "telldus-core.conf.*STATE_INSTALL_DIR"
---

<objective>
Prove that runtime state is completely separate from the stable device configuration and that state persists correctly across daemon restarts.

Purpose: Ensure turning a device on/off writes only to the var config, never to tellstick.conf, and that the daemon recovers state correctly after restart.
Output: Modified install permissions (if needed), state persistence smoke test, and verified permission failure handling.
</objective>

<execution_context>
@/home/rsw/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rsw/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-config-compatibility/04-CONTEXT.md
@.planning/phases/04-config-compatibility/04-RESEARCH.md
@telldus-core/service/SettingsConfuse.cpp
@telldus-core/service/CMakeLists.txt

<interfaces>
From telldus-core/service/SettingsConfuse.cpp:
- setDeviceState() writes to VAR_CONFIG_FILE only
- getDeviceState() reads from VAR_CONFIG_FILE only
- addNode(), removeNode(), setStringSetting(), setIntSetting() write to CONFIG_FILE only
- fopen failure returns false or TELLSTICK_ERROR_PERMISSION_DENIED

From telldus-core/service/CMakeLists.txt:
- telldus-core.conf install permissions: OWNER_READ OWNER_WRITE GROUP_READ GROUP_WRITE WORLD_READ WORLD_WRITE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden install permissions for state file</name>
  <files>telldus-core/service/CMakeLists.txt</files>
  <read_first>
    - telldus-core/service/CMakeLists.txt (lines 260-269 for INSTALL rules)
    - .planning/phases/04-config-compatibility/04-CONTEXT.md (D-04-03: root with 644/755)
    - .planning/phases/04-config-compatibility/04-RESEARCH.md (Security Domain: world-writable state file)
  </read_first>
  <action>
    In telldus-core/service/CMakeLists.txt, tighten the install permissions for telldus-core.conf:
    1. Locate the INSTALL(FILES telldus-core.conf ...) block (around line 265).
    2. The current permissions are OWNER_READ OWNER_WRITE GROUP_READ GROUP_WRITE WORLD_READ WORLD_WRITE (effectively 666).
    3. Change to OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ (effectively 644).
    4. Do NOT change the directory permissions for STATE_INSTALL_DIR (CMake INSTALL(FILES) does not set directory perms; directory creation in main_unix.cpp uses 0755).
    5. Do NOT change tellstick.conf permissions (it is installed via INSTALL(FILES ...) without explicit permissions, so it uses CMake defaults which are typically 644).
    6. If tellstick.conf also has explicit overly permissive permissions, tighten those to OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ as well.
  </action>
  <acceptance_criteria>
    - telldus-core/service/CMakeLists.txt install rule for telldus-core.conf uses OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ
    - The old permission string WORLD_WRITE is removed from the telldus-core.conf install rule
    - FreeBSD or non-Linux install rules are not modified
  </acceptance_criteria>
  <verify>
    <automated>
      grep -A2 'INSTALL(FILES telldus-core.conf' telldus-core/service/CMakeLists.txt | \
      grep -q 'OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ' && \
      ! grep -A2 'INSTALL(FILES telldus-core.conf' telldus-core/service/CMakeLists.txt | \
      grep -q 'WORLD_WRITE' && \
      echo "PASS"
    </automated>
  </verify>
  <done>State file install permissions hardened from 666 to 644.</done>
</task>

<task type="auto">
  <name>Task 2: Verify write target separation in SettingsConfuse.cpp</name>
  <files>telldus-core/service/SettingsConfuse.cpp</files>
  <read_first>
    - telldus-core/service/SettingsConfuse.cpp (lines 101-121 for addNode)
    - telldus-core/service/SettingsConfuse.cpp (lines 149-183 for removeNode)
    - telldus-core/service/SettingsConfuse.cpp (lines 185-224 for setDeviceState)
    - telldus-core/service/SettingsConfuse.cpp (lines 284-314 for setStringSetting)
    - telldus-core/service/SettingsConfuse.cpp (lines 335-361 for setIntSetting)
  </read_first>
  <action>
    Verify and document that write target separation is correct after the path changes in 04-01:
    1. Confirm that addNode(), removeNode(), setStringSetting(), and setIntSetting() all write exclusively to the resolved stable config path (getConfigFilePath() from 04-01).
    2. Confirm that setDeviceState() writes exclusively to the resolved var config path (getVarConfigDir() + "/telldus-core.conf" from 04-01).
    3. If any method incorrectly uses the wrong path variable, fix it.
    4. Add a brief code comment above setDeviceState() stating: "Writes to var config only — stable config is never modified by state changes."
    5. Add a brief code comment above addNode() stating: "Writes to stable config — device definitions are persisted in tellstick.conf."
    6. No functional changes are needed if the paths are already correct; this task is primarily verification and documentation.
  </action>
  <acceptance_criteria>
    - setDeviceState() opens the var config path, not the stable config path
    - addNode(), removeNode(), setStringSetting(), setIntSetting() open the stable config path, not the var config path
    - Code comments clarify which config each method writes to
    - No cross-contamination between stable and var config writes
  </acceptance_criteria>
  <verify>
    <automated>
      grep -n 'setDeviceState' telldus-core/service/SettingsConfuse.cpp | head -1 && \
      sed -n '185,224p' telldus-core/service/SettingsConfuse.cpp | grep -q 'VAR_CONFIG\|varConfig\|getVarConfig' && \
      sed -n '101,121p' telldus-core/service/SettingsConfuse.cpp | grep -q 'CONFIG_FILE\|configPath\|getConfigFile' && \
      echo "PASS"
    </automated>
  </verify>
  <done>Write target separation verified: state changes go to var config, device changes go to stable config.</done>
</task>

<task type="auto">
  <name>Task 3: Create state persistence smoke test</name>
  <files>tests/integration/state-persistence-smoke.sh</files>
  <read_first>
    - tests/integration/config-compat-smoke.sh (from 04-02 for pattern: temp dir, env vars, daemon launch, tdtool)
    - telldus-core/service/SettingsConfuse.cpp (setDeviceState and getDeviceState logic)
    - .planning/phases/04-config-compatibility/04-CONTEXT.md (D-04-06, D-04-10, D-04-11)
  </read_first>
  <action>
    Create tests/integration/state-persistence-smoke.sh that validates state separation and persistence:
    1. Use set -e and a temp directory (mktemp -d).
    2. Copy tests/integration/sample-tellstick.conf to $TMPDIR/tellstick.conf.
    3. Launch telldusd with env vars pointing to $TMPDIR.
    4. Capture the initial MD5 checksum of tellstick.conf.
    5. Use tdtool to turn device 1 ON:
       - TELLDUS_CONFIG_FILE=$TMPDIR/tellstick.conf TELLDUS_STATE_DIR=$TMPDIR ./build/headless/tdtool/tdtool --on 1
       - Assert exit code is 0.
    6. Capture the new MD5 checksum of tellstick.conf.
    7. Assert the two MD5 checksums are IDENTICAL — the stable config must not change when device state changes.
    8. Verify that $TMPDIR/telldus-core.conf exists and is non-empty.
    9. Verify that $TMPDIR/telldus-core.conf contains "state = 1" or equivalent for device 1.
    10. Stop the daemon.
    11. Restart the daemon with the same env vars (simulating a restart).
    12. Run tdtool --list-devices and verify device 1 is shown as ON (state persisted).
    13. Test read-only stable config:
       - chmod 444 $TMPDIR/tellstick.conf
       - Attempt tdtool --off 1 (which may trigger a write if the command changes state)
       - OR attempt to add a new device (which definitely writes to stable config)
       - Assert the operation returns an error (TELLSTICK_ERROR_PERMISSION_DENIED or non-zero exit)
       - Restore permissions with chmod 644
    14. Kill the daemon and clean up.
  </action>
  <acceptance_criteria>
    - tests/integration/state-persistence-smoke.sh exists and is executable
    - Script uses set -e
    - Script verifies stable config MD5 is unchanged after state change
    - Script verifies telldus-core.conf exists after state change
    - Script verifies state persists across daemon restart
    - Script tests read-only config error handling
    - Script cleans up temp directory on exit (trap)
  </acceptance_criteria>
  <verify>
    <automated>
      test -x tests/integration/state-persistence-smoke.sh && \
      grep -q 'set -e' tests/integration/state-persistence-smoke.sh && \
      grep -q 'md5sum\|md5\|sha256sum' tests/integration/state-persistence-smoke.sh && \
      grep -q 'telldus-core.conf' tests/integration/state-persistence-smoke.sh && \
      grep -q 'chmod 444' tests/integration/state-persistence-smoke.sh && \
      grep -q 'kill\|pkill' tests/integration/state-persistence-smoke.sh && \
      echo "PASS"
    </automated>
  </verify>
  <done>State persistence smoke test script validates separation, persistence, and permission errors.</done>
</task>

<task type="auto">
  <name>Task 4: Run state persistence smoke test</name>
  <files>tests/integration/state-persistence-smoke.sh, build/headless/</files>
  <read_first>
    - tests/integration/state-persistence-smoke.sh
    - build/headless/04-01-build.log (confirm daemon binary exists)
  </read_first>
  <action>
    Execute the state persistence smoke test:
    1. Ensure build/headless/service/telldusd and build/headless/tdtool/tdtool exist.
    2. Run: bash tests/integration/state-persistence-smoke.sh 2>&1 | tee tests/integration/state-persistence-smoke.log
    3. If the test fails, analyze:
       - If stable config MD5 changes after tdtool --on: setDeviceState is writing to the wrong file.
       - If state does not persist across restart: var config is not being read on startup, or var config file is missing.
       - If read-only config does not return error: permission check is missing or tdtool exit code is not propagated.
    4. Fix issues in source files or test script, rebuild, and re-run until passing.
    5. Do NOT skip this task on failure. The phase goal requires verified state separation.
  </action>
  <acceptance_criteria>
    - tests/integration/state-persistence-smoke.sh exits 0
    - Log file tests/integration/state-persistence-smoke.log exists
    - Log contains evidence that stable config MD5 did not change
    - Log contains evidence that telldus-core.conf was created and populated
    - Log contains evidence that state persisted across restart
    - Log contains evidence that read-only config returned an error
  </acceptance_criteria>
  <verify>
    <automated>
      test -f tests/integration/state-persistence-smoke.log && \
      grep -q 'PASS\|SUCCESS' tests/integration/state-persistence-smoke.log && \
      echo "PASS"
    </automated>
  </verify>
  <done>State persistence smoke test passes: stable config unchanged, var config persists, permissions enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon -> stable config | Daemon must not write state to tellstick.conf |
| Daemon -> var config | Daemon writes state; file must be readable by daemon on restart |
| Test script -> filesystem | Script manipulates permissions to test error paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | State file 644 allows group/world read | accept | State contains only device on/off values; no secrets. Dedicated user deferred to v2. |
| T-04-03 | Denial of Service | Read-only stable config blocks legitimate writes | mitigate | Return clear error TELLSTICK_ERROR_PERMISSION_DENIED; operator knows the config is read-only |
| T-04-03 | Information Disclosure | tdtool --list-devices shows all device names | accept | Device names are not sensitive; this is existing behavior |
</threat_model>

<verification>
- telldus-core.conf install permissions are 644 (not 666)
- setDeviceState writes only to var config; stable config methods write only to tellstick.conf
- State persistence smoke test exits 0 with all assertions passed
- Read-only stable config returns permission denied error
</verification>

<success_criteria>
- Stable config tellstick.conf is never modified by device state changes
- Runtime state persists in telldus-core.conf across daemon restarts
- Permission errors are propagated correctly when configs are read-only
- State file is installed with secure 644 permissions
</success_criteria>

<output>
After completion, create `.planning/phases/04-config-compatibility/04-03-SUMMARY.md`
</output>
