---
phase: 02-arch-native-build
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - telldus-core/tests/CMakeLists.txt
  - CMakePresets.json
  - telldus-core/tests/cppunit.cpp
  - docs/testing-split.md
autonomous: true
requirements:
  - NBLD-04

must_haves:
  truths:
    - "CppUnit unit tests compile and pass without TellStick hardware"
    - "Style tests (cpplint) and static analysis (cppcheck) are disabled from CTest"
    - "Unit tests and integration tests are explicitly categorized"
    - "Test build is reproducible via the headless CMake preset"
  artifacts:
    - path: "telldus-core/tests/CMakeLists.txt"
      provides: "Enabled CppUnit tests without style/static checks"
      exports: []
    - path: "build/headless/tests/TestRunner"
      provides: "Compiled CppUnit test runner"
      exports: []
    - path: "docs/testing-split.md"
      provides: "Unit vs integration test classification"
      exports: []
  key_links:
    - from: "CMakePresets.json"
      to: "telldus-core/tests/CMakeLists.txt"
      via: "ENABLE_TESTING=TRUE"
      pattern: "ENABLE_TESTING.*TRUE"
    - from: "telldus-core/tests/CMakeLists.txt"
      to: "build/headless/tests/TestRunner"
      via: "ADD_EXECUTABLE(TestRunner ...)"
      pattern: "TestRunner.*cppunit"
    - from: "TestRunner"
      to: "ctest"
      via: "ADD_TEST(cppunit ...)"
      pattern: "ADD_TEST\(cppunit"
---

<objective>
Enable CppUnit tests, fix test failures, and establish the unit vs integration test split.

Purpose: Provide a practical automated test suite that runs on modern Linux without requiring TellStick hardware, creating a CI-friendly verification layer.
Output: Enabled and passing CppUnit tests, disabled style/static checks, and documented test classification.
</objective>

<execution_context>
@/home/rsw/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rsw/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-arch-native-build/02-CONTEXT.md
@.planning/phases/02-arch-native-build/02-PATTERNS.md
@.planning/phases/02-arch-native-build/02-02-SUMMARY.md

<interfaces>
From telldus-core/tests/CMakeLists.txt:
- SET(ENABLE_TESTING FALSE CACHE BOOL "Enable unit tests")
- ADD_SOURCES function registers cpplint as CTest tests
- ADD_TEST(cppunit ${CMAKE_CURRENT_BINARY_DIR}/TestRunner)
- cppcheck test on Unix: ADD_TEST(cppcheck cppcheck --quiet --error-exitcode=2 ${CMAKE_SOURCE_DIR})

From telldus-core/tests/common/:
- CommonTests.h registers StringsTest
- StringsTest.h uses CPPUNIT_TEST_SUITE/CPPUNIT_TEST_SUITE_END

From telldus-core/tests/service/:
- ServiceTests.h registers ProtocolEverflourishTest, ProtocolHastaTest, ProtocolNexaTest, ProtocolOregonTest, ProtocolSartanoTest, ProtocolX10Test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable CppUnit tests and disable style/static checks</name>
  <files>telldus-core/tests/CMakeLists.txt, CMakePresets.json</files>
  <read_first>
    - telldus-core/tests/CMakeLists.txt (current test registration and ENABLE_TESTING default)
    - telldus-core/CMakeLists.txt (how ENABLE_TESTING is used in subdirectory and root)
    - CMakePresets.json (current headless preset cache variables)
  </read_first>
  <action>
    In telldus-core/tests/CMakeLists.txt:
    - Change SET(ENABLE_TESTING FALSE CACHE BOOL "Enable unit tests") to SET(ENABLE_TESTING TRUE CACHE BOOL "Enable unit tests")
    - Remove or comment out the ADD_SOURCES function calls that register cpplint style tests (the calls on the lines matching ADD_SOURCES(TelldusCommon ...), ADD_SOURCES(${telldus-core_TARGET} ...), ADD_SOURCES(${telldus-service_TARGET} ...))
    - Remove or comment out the cppcheck ADD_TEST line (IF (UNIX) ADD_TEST(cppcheck ...) ENDIF())
    - Keep the ADD_TEST(cppunit ${CMAKE_CURRENT_BINARY_DIR}/TestRunner) line intact
    - Keep the ADD_SOURCES FUNCTION definition and the cpplint_filters variable (they may be reused later)
    In CMakePresets.json:
    - Update the headless preset cacheVariables to set ENABLE_TESTING: TRUE
    Per D-02-10: enable CppUnit unit tests only, skip cpplint and cppcheck for now.
  </action>
  <verify>
    <automated>grep -c "ADD_TEST(cppunit" telldus-core/tests/CMakeLists.txt &amp;&amp; grep -c "ENABLE_TESTING.*TRUE" CMakePresets.json &amp;&amp; ! grep -q "ADD_TEST(.*cpplint" telldus-core/tests/CMakeLists.txt &amp;&amp; ! grep -q "ADD_TEST(.*cppcheck" telldus-core/tests/CMakeLists.txt</automated>
  </verify>
  <done>tests/CMakeLists.txt enables testing, removes cpplint/cppcheck from CTest, and preset reflects ENABLE_TESTING=TRUE</done>
  <acceptance_criteria>
    - telldus-core/tests/CMakeLists.txt contains ENABLE_TESTING set to TRUE or no longer hardcodes FALSE
    - ADD_TEST(cppunit ...) remains in telldus-core/tests/CMakeLists.txt
    - cpplint ADD_TEST registrations are removed or commented out
    - cppcheck ADD_TEST is removed or commented out
    - CMakePresets.json headless preset has ENABLE_TESTING: TRUE
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix test compilation and runtime failures</name>
  <files>telldus-core/tests/cppunit.cpp, telldus-core/tests/common/StringsTest.cpp, telldus-core/tests/service/ProtocolEverflourishTest.cpp, telldus-core/tests/service/ProtocolHastaTest.cpp, telldus-core/tests/service/ProtocolNexaTest.cpp, telldus-core/tests/service/ProtocolOregonTest.cpp, telldus-core/tests/service/ProtocolSartanoTest.cpp, telldus-core/tests/service/ProtocolX10Test.cpp</files>
  <read_first>
    - telldus-core/tests/cppunit.cpp (test runner main)
    - telldus-core/tests/common/CMakeLists.txt (common test build config)
    - telldus-core/tests/service/CMakeLists.txt (service test build config)
    - telldus-core/tests/common/CommonTests.h (test suite registration)
    - telldus-core/tests/service/ServiceTests.h (test suite registration)
    - Each Protocol*Test.cpp file listed above
  </read_first>
  <action>
    Reconfigure the build to pick up test changes: cmake --preset headless
    Build the test runner: cmake --build build/headless --target TestRunner
    Fix any compilation errors in test source files or test CMakeLists.txt files.
    Run the tests: ctest --test-dir build/headless -R cppunit --output-on-failure
    Or run directly: ./build/headless/tests/TestRunner
    Fix all failing CppUnit assertions. Do not modify production code behavior to make tests pass unless the production code is demonstrably buggy and the test validates correct behavior.
    Per D-02-09, D-02-11: enable all tests and fix everything that fails inline during Phase 2.
  </action>
  <verify>
    <automated>ctest --test-dir build/headless -R cppunit --output-on-failure</automated>
  </verify>
  <done>TestRunner compiles and all CppUnit tests pass</done>
  <acceptance_criteria>
    - cmake --build build/headless --target TestRunner exits 0
    - build/headless/tests/TestRunner exists and is executable
    - ctest --test-dir build/headless -R cppunit exits 0
    - TestRunner exits 0 when executed directly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document unit vs integration test split</name>
  <files>docs/testing-split.md</files>
  <read_first>
    - telldus-core/tests/common/ (all test headers and implementations)
    - telldus-core/tests/service/ (all test headers and implementations)
    - telldus-core/tests/common/CommonTests.h
    - telldus-core/tests/service/ServiceTests.h
  </read_first>
  <action>
    Create docs/testing-split.md with two sections: Unit Tests and Integration Tests.
    For each existing test (StringsTest, ProtocolEverflourishTest, ProtocolHastaTest, ProtocolNexaTest, ProtocolOregonTest, ProtocolSartanoTest, ProtocolX10Test), classify it as unit or integration and explain why.
    Unit tests are those that exercise pure logic, encode/decode algorithms, or string utilities with no hardware dependency.
    Integration tests are those that require a TellStick USB device, real controller, socket I/O, or external state.
    If no integration tests currently exist, state that explicitly and describe what future integration tests would look like (e.g., USB detection, device on/off with real hardware).
    Per D-02-12: split tests so unit tests run without hardware and integration tests are reserved for Phase 7 hardware verification.
  </action>
  <verify>
    <automated>test -f docs/testing-split.md && grep -c "Unit Tests" docs/testing-split.md && grep -c "Integration Tests" docs/testing-split.md</automated>
  </verify>
  <done>docs/testing-split.md exists and classifies every existing test</done>
  <acceptance_criteria>
    - docs/testing-split.md exists
    - Contains a "Unit Tests" section
    - Contains an "Integration Tests" section
    - Every existing CppUnit test is listed with its classification and a one-line reason
    - Document states that unit tests do not require a TellStick Duo to pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> production code | Tests must not weaken production security to pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Test fixes changing production behavior | mitigate | Only change production code if the test reveals a real bug; otherwise fix the test |
</threat_model>

<verification>
- ctest --test-dir build/headless -R cppunit passes
- TestRunner binary exists and runs without hardware
- docs/testing-split.md documents the split
</verification>

<success_criteria>
- CppUnit tests are enabled and passing
- cpplint and cppcheck are not registered as CTest tests
- All existing tests pass without a TellStick connected
- docs/testing-split.md clearly separates unit and integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-arch-native-build/02-03-SUMMARY.md`
</output>
