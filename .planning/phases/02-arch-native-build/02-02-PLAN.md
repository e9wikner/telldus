---
phase: 02-arch-native-build
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - telldus-core/CMakeLists.txt
  - telldus-core/common/Strings.cpp
  - telldus-core/service/SettingsConfuse.cpp
  - telldus-core/service/TellStick_libftdi.cpp
  - telldus-core/client/Client.cpp
  - telldus-core/tdtool/main.cpp
autonomous: true
requirements:
  - NBLD-02

must_haves:
  truths:
    - "telldusd, libtelldus-core, and tdtool compile without errors on Arch Linux"
    - "Build completes with zero critical warnings (deprecated, conversion, return-type)"
    - "Warning flags are enforced globally for all future builds"
  artifacts:
    - path: "telldus-core/CMakeLists.txt"
      provides: "Global compiler warning flags"
      exports: []
    - path: "build/headless/service/telldusd"
      provides: "Compiled daemon binary"
      exports: []
    - path: "build/headless/client/libtelldus-core.so"
      provides: "Compiled shared library"
      exports: []
    - path: "build/headless/tdtool/tdtool"
      provides: "Compiled CLI binary"
      exports: []
  key_links:
    - from: "telldus-core/CMakeLists.txt"
      to: "compiler"
      via: "ADD_COMPILE_OPTIONS"
      pattern: "ADD_COMPILE_OPTIONS.*-Wall.*-Wextra"
    - from: "source files"
      to: "build/headless/"
      via: "cmake --build build/headless"
      pattern: "telldusd.*libtelldus-core.*tdtool"
---

<objective>
Fix all compiler errors and critical warnings so the headless build completes cleanly.

Purpose: Modern compilers and libraries expose legacy code issues. Fixing them now creates a clean baseline for later phases and prevents warning fatigue.
Output: Warning-enforced CMake configuration and zero-error build of telldusd, libtelldus-core, and tdtool.
</objective>

<execution_context>
@/home/rsw/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rsw/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-arch-native-build/02-CONTEXT.md
@.planning/phases/02-arch-native-build/02-PATTERNS.md
@.planning/phases/02-arch-native-build/02-01-SUMMARY.md

<interfaces>
From telldus-core/CMakeLists.txt:
- CMAKE_MINIMUM_REQUIRED( VERSION 2.6.0...3.24.3 )
- Existing compile flags pattern in client/CMakeLists.txt: SET_TARGET_PROPERTIES( ${telldus-core_TARGET} PROPERTIES COMPILE_FLAGS "-fPIC -fvisibility=hidden")

From 02-PATTERNS.md:
- Strings.cpp toupper warning: use static_cast<int(*)(int)>(toupper) or lambda
- TellStick_libftdi.cpp reinterpret_cast: pass array buf instead of &buf
- Client.cpp strncpy: add explicit null-termination after strncpy
- tdtool/main.cpp strcat: replace with strncat or std::string, initialize buffers with {0}
- SettingsConfuse.cpp const_cast: use char arrays or documented pragma suppression only if unavoidable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compiler warning flags to root CMakeLists.txt</name>
  <files>telldus-core/CMakeLists.txt</files>
  <read_first>
    - telldus-core/CMakeLists.txt (current state, especially CMAKE_MINIMUM_REQUIRED location)
    - telldus-core/client/CMakeLists.txt (existing compile flags pattern with SET_TARGET_PROPERTIES)
    - .planning/phases/02-arch-native-build/02-PATTERNS.md (shared compiler warning flags pattern)
  </read_first>
  <action>
    In telldus-core/CMakeLists.txt, immediately after the CMAKE_MINIMUM_REQUIRED line, add:
    IF(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
      ADD_COMPILE_OPTIONS(
        -Wall
        -Wextra
        -Wdeprecated-declarations
        -Wconversion
        -Wsign-conversion
        -Werror=deprecated-declarations
        -Werror=return-type
      )
    ENDIF()
    Preserve all existing CMake logic. Do not remove or alter existing options or subdirectory additions.
    Per D-02-08: flags added directly to CMakeLists.txt to enforce the standard for all future builds.
  </action>
  <verify>
    <automated>grep -c "ADD_COMPILE_OPTIONS" telldus-core/CMakeLists.txt && grep -c "Werror=deprecated-declarations" telldus-core/CMakeLists.txt</automated>
  </verify>
  <done>Global warning flags block exists in telldus-core/CMakeLists.txt inside GNUCXX/Clang conditional</done>
  <acceptance_criteria>
    - telldus-core/CMakeLists.txt contains ADD_COMPILE_OPTIONS
    - The options include: -Wall, -Wextra, -Wdeprecated-declarations, -Wconversion, -Wsign-conversion, -Werror=deprecated-declarations, -Werror=return-type
    - The ADD_COMPILE_OPTIONS block is guarded by IF(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
    - No existing CMake logic was removed or altered
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix compilation errors and critical warnings in headless sources</name>
  <files>
    telldus-core/common/Strings.cpp
    telldus-core/service/SettingsConfuse.cpp
    telldus-core/service/TellStick_libftdi.cpp
    telldus-core/client/Client.cpp
    telldus-core/tdtool/main.cpp
  </files>
  <read_first>
    - build/headless/initial-build-errors.log (from 02-01, the complete compiler output)
    - .planning/phases/02-arch-native-build/02-PATTERNS.md (specific warning fix patterns for each file)
    - Each source file listed above (read current content before editing)
  </read_first>
  <action>
    Rebuild with the new warning flags from Task 1 to generate a fresh build log: cmake --build build/headless 2>&1 | tee build/headless/warning-fix-build.log
    Fix every distinct compiler error and every warning in the categories -Wdeprecated-declarations, -Wconversion, -Wsign-conversion, -Wreturn-type from this fresh build. Apply fixes per 02-PATTERNS.md:
    - Strings.cpp: Replace toupper in std::transform with static_cast<int(*)(int)>(toupper) or [](unsigned char c){ return std::toupper(c); }
    - TellStick_libftdi.cpp: Fix reinterpret_cast by passing buf (the array) instead of &buf (address of array) to processData
    - Client.cpp: After every strncpy call, add explicit null-termination: buffer[len-1] = '\0'; or use snprintf
    - tdtool/main.cpp: Replace strcat with strncat or std::string operations; initialize char buffers with {0} instead of buf[0] = 0
    - SettingsConfuse.cpp: Address const_cast warnings. Prefer replacing const_cast<char*>("literal") with local char arrays (char id_str[] = "id";). Only use pragma suppression if the libconfuse macro signature absolutely prevents a clean fix.
    Preserve tab indentation in all modified files. Do not suppress warnings with pragmas as a shortcut.
    Per D-02-05, D-02-06, D-02-07: fix errors and critical warnings, do not leave technical debt.
  </action>
  <verify>
    <automated>test "$(cmake --build build/headless 2>&1 | grep -c "error:" || echo 0)" -eq 0 &amp;&amp; test "$(cmake --build build/headless 2>&1 | grep -cE "-Wdeprecated-declarations|-Wconversion|-Wsign-conversion|-Wreturn-type" || echo 0)" -eq 0</automated>
  </verify>
  <done>Build produces zero errors and zero warnings in the enforced categories</done>
  <acceptance_criteria>
    - Build output contains zero lines matching "error:"
    - Build output contains zero warnings matching -Wdeprecated-declarations, -Wconversion, -Wsign-conversion, or -Wreturn-type
    - Each file modified uses the same tab indentation style as before
    - No pragma warning suppressions were added unless absolutely required by external API
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify clean build of all headless targets</name>
  <files>build/headless/service/telldusd, build/headless/client/libtelldus-core.so, build/headless/tdtool/tdtool</files>
  <read_first>
    - telldus-core/CMakeLists.txt (target names and output locations)
    - telldus-core/service/CMakeLists.txt (telldusd target definition)
    - telldus-core/client/CMakeLists.txt (libtelldus-core target definition)
  </read_first>
  <action>
    Run cmake --build build/headless from repo root. Verify the build exits with code 0.
    Check that these artifacts exist and are executable (for binaries) or are shared libraries:
    - build/headless/service/telldusd (executable)
    - build/headless/client/libtelldus-core.so or libtelldus-core.so.2 (shared library)
    - build/headless/tdtool/tdtool (executable)
    If any artifact is missing, trace the CMake target and build log to identify why.
  </action>
  <verify>
    <automated>test -x build/headless/service/telldusd &amp;&amp; ls build/headless/client/libtelldus-core.so* >/dev/null 2>&1 &amp;&amp; test -x build/headless/tdtool/tdtool</automated>
  </verify>
  <done>All three headless targets (telldusd, libtelldus-core, tdtool) are built successfully</done>
  <acceptance_criteria>
    - cmake --build build/headless exits 0
    - build/headless/service/telldusd exists and is executable
    - build/headless/client/libtelldus-core.so* exists (any version suffix)
    - build/headless/tdtool/tdtool exists and is executable
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compiler -> source code | Compiler flags affect code generation but do not change semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Source modifications for warning fixes | mitigate | Review each change against 02-PATTERNS.md; preserve behavior, only fix types/casts/initializations |
</threat_model>

<verification>
- cmake --build build/headless exits 0
- Zero compiler errors in build output
- Zero warnings in enforced categories
- All three headless binary artifacts exist
</verification>

<success_criteria>
- Global warning flags are active in telldus-core/CMakeLists.txt
- Build completes with zero errors
- No critical warnings remain (deprecated-declarations, conversion, sign-conversion, return-type)
- telldusd, libtelldus-core.so, and tdtool are present in build/headless/
</success_criteria>

<output>
After completion, create `.planning/phases/02-arch-native-build/02-02-SUMMARY.md`
</output>
