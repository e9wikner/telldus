---
phase: 07-tellstick-duo-hardware-verification
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - docs/verification-checklist.md
  - docs/hardware-verification.md
  - scripts/verify-tellstick-hardware.sh
autonomous: false
requirements:
  - DOCS-04
user_setup: []
must_haves:
  truths:
    - "Operator has a step-by-step manual verification checklist"
    - "Checklist includes sign-off section for recording results"
    - "Checklist covers USB detection, device listing, on/off commands, error paths"
    - "Documentation is complete and suitable for end-user operation"
  artifacts:
    - path: "docs/verification-checklist.md"
      provides: "Manual hardware verification checklist with sign-off"
      contains: "Numbered test steps, expected results, sign-off table"
    - path: "docs/hardware-verification.md"
      provides: "Complete hardware verification documentation"
  key_links:
    - from: "docs/verification-checklist.md"
      to: "scripts/verify-tellstick-hardware.sh"
      via: "Reference to automated script"
      pattern: "verify-tellstick-hardware.sh"
---

<objective>
Create the manual hardware verification checklist (DOCS-04) that operators use to validate TellStick Duo functionality. This is the final deliverable of Phase 7 - comprehensive documentation that enables reproducible hardware verification.

Purpose: Per D-07-01, D-07-10, and DOCS-04 requirement, operators need a clear, step-by-step checklist to verify their TellStick Duo setup. This plan produces the operator-facing documentation that combines automated scripts with manual verification steps.

Output:
- `docs/verification-checklist.md` - Complete manual verification checklist
- Finalized `docs/hardware-verification.md` - Complete hardware verification guide
- Updated `scripts/verify-tellstick-hardware.sh` with final polish
</objective>

<execution_context>
@.planning/phases/07-tellstick-duo-hardware-verification/07-CONTEXT.md
@.planning/phases/07-tellstick-duo-hardware-verification/07-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/VERIFICATION.md (from Phase 6 for format reference)
@scripts/verify-tellstick-hardware.sh (from 07-02)
@scripts/verify-error-paths.sh (from 07-03)

## Key Technical Context

### Manual Verification Requirements (DOCS-04)
Per REQUIREMENTS.md: "Operator has a concise manual verification checklist for TellStick Duo behavior"

### Checklist Structure Requirements
Per 07-RESEARCH.md Pattern 4 (Hybrid Verification):
- Executable script for automated checks
- Manual checklist for hardware-specific steps

### Scope Decisions to Document
- D-07-03: Docker as primary target (not native Arch)
- D-07-05: Docker container with USB passthrough is primary
- D-07-08: On/off commands only (dimming out of scope)
- D-07-09: Test one representative device
- D-07-10: On → verify → Off → verify cycle
- D-07-11: Error path verification when hardware absent

### Existing Documentation to Reference
- docs/VERIFICATION.md from Phase 6 (format reference)
- docs/docker-runtime.md (for run commands)
- docs/hardware-verification.md (technical details from 07-01, 07-02, 07-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive verification checklist</name>
  <files>docs/verification-checklist.md</files>
  <read_first>
    - docs/VERIFICATION.md (Phase 6 format)
    - docs/hardware-verification.md (technical details from prior plans)
    - .planning/phases/07-tellstick-duo-hardware-verification/07-RESEARCH.md (hybrid verification pattern)
  </read_first>
  <acceptance_criteria>
    - File docs/verification-checklist.md is created with comprehensive checklist
    - File includes header with title, version, and prerequisites
    - File includes Prerequisites section:
      - Docker installed and running
      - TellStick Duo connected to USB
      - tellstick.conf exists and configured
      - telldus:latest image built
    - File includes numbered test sections (minimum 6 tests):
      1. Container Startup - Run container with proper USB passthrough
      2. USB Detection - Verify TellStick visible in container
      3. Device Listing - tdtool --list shows configured devices
      4. On Command - tdtool --on <id> succeeds
      5. Off Command - tdtool --off <id> succeeds
      6. Error Path - Verify graceful failure without hardware (optional)
    - Each test includes:
      - Test objective
      - Prerequisites for that test
      - Step-by-step commands to execute
      - Expected results (specific output patterns)
      - Pass/fail criteria
      - Troubleshooting tips
    - File includes Sign-Off section with table:
      - Columns: Test #, Description, Result (PASS/FAIL/N/A), Notes, Date, Initials
      - Rows for each numbered test
      - Final sign-off line: "Verification completed by: _________ Date: _________"
    - File includes Quick Reference section with:
      - Common docker commands
      - Common tdtool commands
      - Where to find logs
    - File references automated scripts where applicable
  </acceptance_criteria>
  <action>Create docs/verification-checklist.md as comprehensive operator checklist:

1. Header with:
   - Title: "TellStick Duo Hardware Verification Checklist"
   - Phase reference: Phase 7
   - Version and date
   - Prerequisites summary

2. Prerequisites section:
   - Hardware: TellStick Duo connected via USB
   - Software: Docker installed, telldus image built
   - Configuration: tellstick.conf exists with at least one device
   - Access: Docker permissions for operator

3. Test 1: Container Startup
   - Objective: Start container with USB passthrough
   - Steps: docker run with --privileged, --device, -v for config
   - Expected: Container starts without errors
   - Verify: docker ps shows running container

4. Test 2: USB Device Detection
   - Objective: Verify TellStick Duo visible in container
   - Steps: docker exec telldus lsusb | grep "1781:0c31"
   - Expected: Output shows "Telldus TellStick Duo"
   - Reference: scripts/verify-usb-detection.sh

5. Test 3: Device Listing (DUO-03)
   - Objective: List configured devices
   - Steps: docker exec telldus tdtool --list
   - Expected: Shows device count and list from tellstick.conf
   - Verify: Device names match config

6. Test 4: On Command (DUO-04)
   - Objective: Transmit ON command
   - Steps: docker exec telldus tdtool --on 1
   - Expected: Exit code 0, "Turning on device X" output
   - Warning: 433 MHz is one-way - physically verify device

7. Test 5: Off Command (DUO-04)
   - Objective: Transmit OFF command
   - Steps: docker exec telldus tdtool --off 1
   - Expected: Exit code 0, "Turning off device X" output
   - Per D-07-10: Complete on→off cycle

8. Test 6: Automated Verification (Optional)
   - Objective: Run automated verification script
   - Steps: ./scripts/verify-tellstick-hardware.sh
   - Expected: All automated tests pass
   - Note: Manual verification still required for physical confirmation

9. Sign-Off table:
   - Create markdown table with columns: Test #, Description, Result, Notes, Date, Initials
   - Include all 6 tests
   - Add final signature line

10. Quick Reference:
    - Container management commands
    - tdtool command reference
    - Log access: docker logs telldus
    - Script locations

Follow format from docs/VERIFICATION.md with clear sections and code blocks.</action>
  <verify>
    <automated>test -f docs/verification-checklist.md && grep -q "TellStick Duo Hardware Verification Checklist" docs/verification-checklist.md && grep -c "^## Test [0-9]" docs/verification-checklist.md | awk '{exit ($1 < 5) ? 1 : 0}' && grep -q "Sign-Off" docs/verification-checklist.md && grep -q "PASS.*FAIL.*N/A" docs/verification-checklist.md</automated>
  </verify>
  <done>
    - docs/verification-checklist.md exists with comprehensive checklist
    - At least 5 numbered test sections present
    - Sign-off table with PASS/FAIL/N/A columns
    - Quick reference section included
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize hardware verification documentation</name>
  <files>docs/hardware-verification.md</files>
  <read_first>
    - docs/hardware-verification.md (current state from 07-01, 07-02, 07-03)
    - docs/verification-checklist.md (new checklist)
  </read_first>
  <acceptance_criteria>
    - docs/hardware-verification.md is complete and polished
    - File includes all sections from prior plans:
      - USB Detection Verification (from 07-01)
      - Device Command Verification (from 07-02)
      - Error Path Verification (from 07-03)
    - File includes new sections:
      - Overview/Introduction explaining Phase 7 scope
      - Prerequisites (Docker, TellStick, config file)
      - Quick Start guide with minimal commands
      - Automated vs Manual Verification comparison
      - References to verification-checklist.md
    - File includes Summary section:
      - What was verified (USB, listing, on/off, errors)
      - What was NOT verified (dimming per D-07-08, sensors per D-07-04)
      - Next steps (Phase 8 documentation)
    - File has consistent formatting and navigation links
    - All code blocks are properly fenced
    - All cross-references use relative paths
  </acceptance_criteria>
  <action>Finalize docs/hardware-verification.md:

1. Add/Update Overview section at top:
   - Phase 7 context
   - Scope summary (Docker primary per D-07-03)
   - What's covered vs what's deferred

2. Add Prerequisites section:
   - Docker installed
   - telldus:latest image
   - TellStick Duo hardware
   - tellstick.conf with configured devices

3. Add Quick Start section:
   - Minimal commands to get started
   - Reference to full checklist

4. Add Automated vs Manual section:
   - Automated: ./scripts/verify-tellstick-hardware.sh
   - Manual: docs/verification-checklist.md
   - When to use each

5. Ensure existing sections are polished:
   - USB Detection (07-01)
   - Device Commands (07-02)
   - Error Paths (07-03)

6. Add Summary section:
   - Requirements covered: DUO-01, DUO-03, DUO-04, DOCS-04
   - Requirements deferred: DUO-05 (dimming), DUO-06 (sensors) per decisions
   - Reference to Phase 8 documentation

7. Add navigation footer:
   - Next: Phase 8 Operator Documentation
   - Related: docker-runtime.md, VERIFICATION.md

Review entire file for consistency and completeness.</action>
  <verify>
    <automated>grep -q "Overview" docs/hardware-verification.md && grep -q "Prerequisites" docs/hardware-verification.md && grep -q "Quick Start" docs/hardware-verification.md && grep -q "verification-checklist.md" docs/hardware-verification.md && grep -q "Summary" docs/hardware-verification.md</automated>
  </verify>
  <done>
    - docs/hardware-verification.md is complete with all required sections
    - Overview, Prerequisites, Quick Start sections present
    - References verification-checklist.md
    - Summary notes deferred requirements
  </done>
</task>

<task type="auto">
  <name>Task 3: Polish verification script with final features</name>
  <files>scripts/verify-tellstick-hardware.sh</files>
  <read_first>
    - scripts/verify-tellstick-hardware.sh (current state)
    - docs/verification-checklist.md (to align output)
  </read_first>
  <acceptance_criteria>
    - scripts/verify-tellstick-hardware.sh updated with final polish
    - Script includes --summary flag that prints checklist-aligned output
    - Script output format matches checklist structure:
      - Test numbers align with checklist Test #
      - Clear PASS/FAIL for each test
      - Section headers matching checklist sections
    - Script includes --verbose flag for detailed output
    - Script includes helpful error messages for common issues:
      - "Container not running: run ./scripts/run-telldus.sh first"
      - "USB not detected: check cable and --privileged flag"
      - "Device not found: verify device ID exists in tellstick.conf"
    - Script header updated with reference to verification-checklist.md
    - Script tested with bash -n (syntax check)
  </acceptance_criteria>
  <action>Polish scripts/verify-tellstick-hardware.sh:

1. Update header comments:
   - Add reference to docs/verification-checklist.md
   - Document --summary and --verbose flags
   - Add example usage

2. Add argument parsing enhancements:
   - --summary: Print condensed results suitable for checklist
   - --verbose: Print detailed output including command traces
   - --help: Comprehensive help with examples
   - Maintain existing positional args [container] [device_id]

3. Update output format:
   - Add test numbers: "[Test 1] USB Detection"
   - Match numbering to verification-checklist.md
   - Clear section headers

4. Enhance error messages:
   - Container not running → suggest ./scripts/run-telldus.sh
   - USB not detected → suggest checking --privileged
   - Device not found → suggest checking tellstick.conf
   - Include troubleshooting command suggestions

5. Add summary output function:
   - Print table-like results
   - Show test number, name, result
   - Suitable for copying to checklist

6. Verify syntax:
   - Run bash -n to check syntax
   - Ensure all functions defined before use

Maintain backward compatibility - existing usage patterns still work.</action>
  <verify>
    <automated>bash -n scripts/verify-tellstick-hardware.sh && grep -q "\-\-summary" scripts/verify-tellstick-hardware.sh && grep -q "\-\-verbose" scripts/verify-tellstick-hardware.sh && grep -q "verification-checklist.md" scripts/verify-tellstick-hardware.sh</automated>
  </verify>
  <done>
    - Script updated with --summary and --verbose flags
    - Error messages reference related scripts and docs
    - Output format aligns with checklist
    - Syntax verified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Operator | Checklist instructions guide operator actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Repudiation | Operator claims verification not documented | mitigate | Sign-off table provides accountability |
| T-07-04-02 | Information Disclosure | Checklist reveals device IDs/config | accept | Checklist is operator-facing documentation for their own system |
</threat_model>

<verification>
## Phase 07-04 Verification

1. **Checklist exists:** `test -f docs/verification-checklist.md`
2. **Sign-off table:** `grep -q "Sign-Off" docs/verification-checklist.md && grep -q "PASS.*FAIL.*N/A" docs/verification-checklist.md`
3. **Test count:** At least 5 numbered tests
4. **Hardware docs complete:** `grep -q "Overview" docs/hardware-verification.md && grep -q "Summary" docs/hardware-verification.md`
5. **Script flags:** `grep -q "\-\-summary" scripts/verify-tellstick-hardware.sh`
</verification>

<success_criteria>
- docs/verification-checklist.md exists with comprehensive manual checklist
- Checklist includes sign-off table with PASS/FAIL/N/A options
- docs/hardware-verification.md is complete and polished
- scripts/verify-tellstick-hardware.sh has --summary and --verbose flags
- Plan addresses requirement DOCS-04 (manual verification checklist)
</success_criteria>

<output>
After completion, create `.planning/phases/07-tellstick-duo-hardware-verification/07-04-SUMMARY.md`
</output>
