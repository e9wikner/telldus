---
phase: 1
plan: 01-01
title: Audit Headless Build Boundary
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
autonomous: true
requirements: [NBLD-01]
requirements_addressed: [NBLD-01]
must_haves:
  truths:
    - "D-01: `telldus-core/` remains the Phase 1 build root."
    - "D-02: The boundary includes `telldusd`, `libtelldus-core` / `telldus-core`, `tdtool`, and the test build boundary."
    - "D-04: Allowed dependencies are minimal and headless."
    - "D-09: Bindings and examples are out of Phase 1 scope."
    - "D-13: The boundary record must help Phase 2 reproduce proof commands without guessing."
  artifacts:
    - path: .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      provides: "Headless target, dependency, exclusion, CMake control, risk, and proof-command boundary record."
  key_links: []
---

<objective>
Create a committed boundary record for Phase 1 that names the exact Linux headless targets, allowed dependencies, excluded components, and current CMake risk points before any build-file edits.
</objective>

## Decision Coverage

- D-01: Keep `telldus-core/` as the build root for Phase 1. Do not add a repo-root build entry point in this phase.
- D-02: The Phase 1 boundary includes `telldusd`, `libtelldus-core`, `tdtool`, and the test build boundary. Actual test execution and fixes primarily belong to Phase 2.
- D-04: The headless target should use minimal required dependencies only: CMake, C/C++ toolchain, pthreads, libconfuse, libftdi/libusb stack, and test tools only when tests are enabled.
- D-09: Ignore bindings and examples in Phase 1. The phase is scoped to core service/client/tdtool/test boundary, not binding/example compatibility.
- D-13: Produce committed docs/build instructions with exact headless configure/build commands so Phase 2 can reproduce the boundary without guessing.

<threat_model>
## Threat Model

No user-facing security boundary is introduced in this plan. The main risks are operational correctness risks:
- **T-01:** Accidentally expanding scope to GUI, bindings, Docker runtime, or hardware validation.
- **T-02:** Misstating dependencies so later build work installs or requires unnecessary packages.
- **Mitigation:** Boundary doc must cite exact source files and list excluded components explicitly.
</threat_model>

<tasks>
  <task id="01-01-01" type="execute">
    <name>Audit current CMake target boundary</name>
    <title>Audit current CMake target boundary</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-CONTEXT.md
      - .planning/phases/01-headless-build-boundary/01-RESEARCH.md
      - .planning/phases/01-headless-build-boundary/01-PATTERNS.md
      - telldus-core/CMakeLists.txt
      - telldus-core/service/CMakeLists.txt
      - telldus-core/client/CMakeLists.txt
      - telldus-core/tdtool/CMakeLists.txt
      - telldus-core/tests/CMakeLists.txt
    </read_first>
    <action>
      Create `.planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md`. Include sections named `Headless Targets`, `Allowed Dependencies`, `Excluded Components`, `Current CMake Controls`, `Known Boundary Risks`, and `Proof Commands`. In `Headless Targets`, list exactly `telldusd`, `telldus-core`, `tdtool`, and optional `TestRunner`/test targets when `ENABLE_TESTING=TRUE`. In `Excluded Components`, list `telldus-gui/`, TelldusCenter, Qt GUI dependencies, bindings, examples, Docker runtime, Raspberry Pi portability, hardware validation, MQTT, and Home Assistant integration.
    </action>
    <verify>
      test -f .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      rg -n "telldusd|telldus-core|tdtool|ENABLE_TESTING|telldus-gui|Qt|Docker|MQTT" .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
    </verify>
    <acceptance_criteria>
      - `.planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md` exists.
      - The file contains headings `## Headless Targets`, `## Allowed Dependencies`, `## Excluded Components`, `## Current CMake Controls`, `## Known Boundary Risks`, and `## Proof Commands`.
      - The file contains the strings `telldusd`, `telldus-core`, `tdtool`, `ENABLE_TESTING`, `telldus-gui/`, `Qt`, `Docker`, and `MQTT`.
    </acceptance_criteria>
  </task>

  <task id="01-01-02" type="execute">
    <name>Record local prerequisite state</name>
    <title>Record local prerequisite state</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-RESEARCH.md
      - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
    </read_first>
    <action>
      Update `.planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md` with a `Local Probe` subsection. Record that `/usr/bin/gcc`, `/usr/bin/g++`, and `/usr/bin/pkg-config` were found, and that `cmake` was not found in this environment during planning. Include the exact failed configure command from `01-RESEARCH.md`.
    </action>
    <verify>
      rg -n "Local Probe|/usr/bin/gcc|/usr/bin/g\\+\\+|/usr/bin/pkg-config|cmake.*not found|cmake -S telldus-core" .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
    </verify>
    <acceptance_criteria>
      - `01-BUILD-BOUNDARY.md` contains heading `## Local Probe`.
      - `01-BUILD-BOUNDARY.md` contains `/usr/bin/gcc`, `/usr/bin/g++`, `/usr/bin/pkg-config`, and `cmake`.
      - `01-BUILD-BOUNDARY.md` contains `cmake -S telldus-core`.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `test -f .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md`
- `rg -n "Headless Targets|Allowed Dependencies|Excluded Components|Current CMake Controls|Known Boundary Risks|Proof Commands|Local Probe" .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md`
</verification>

<must_haves>
  <truths>
    - D-01: `telldus-core/` remains the Phase 1 build root.
    - D-02: The boundary includes `telldusd`, `libtelldus-core` / `telldus-core`, `tdtool`, and the test build boundary.
    - D-04: Allowed dependencies are minimal and headless.
    - D-09: Bindings and examples are out of Phase 1 scope.
    - D-13: The boundary record must help Phase 2 reproduce proof commands without guessing.
  </truths>
</must_haves>

<success_criteria>
- Developer can read one committed boundary document and know exactly what Phase 1 is allowed to touch.
- The document names the headless targets, allowed dependencies, excluded components, and current local prerequisite blocker.
</success_criteria>
