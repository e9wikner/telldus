---
phase: 1
plan: 01-02
title: Adjust Headless CMake Boundary
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - telldus-core/CMakeLists.txt
  - telldus-core/service/CMakeLists.txt
  - telldus-core/client/CMakeLists.txt
  - telldus-core/tdtool/CMakeLists.txt
  - telldus-core/tests/CMakeLists.txt
autonomous: true
requirements: [NBLD-01]
requirements_addressed: [NBLD-01]
must_haves:
  truths:
    - "D-01: `telldus-core/` remains the Phase 1 build root."
    - "D-03: Add an explicit CMake option/profile only if existing options are too unclear."
    - "D-04: Keep minimal headless dependencies only."
    - "D-05: Prefer libftdi; keep ftd2xx only if harmless."
    - "D-06: Remove accidental Qt/TelldusCenter coupling if found."
    - "D-07: Guard non-Linux paths; do not modernize or delete them."
    - "D-08: Use the least invasive CMake fix that unblocks modern Linux."
    - "D-12: Fix only blockers directly tied to defining the headless boundary."
  artifacts: []
  key_links: []
---

<objective>
Make the `telldus-core/` CMake build surface express the Linux headless boundary clearly enough to configure and dry-build without TelldusCenter/Qt dependencies.
</objective>

## Decision Coverage

- D-01: Keep `telldus-core/` as the build root for Phase 1. Do not add a repo-root build entry point in this phase.
- D-03: Inspect existing CMake options first. Add an explicit CMake option/profile only if the existing build interface is too unclear to make the headless boundary repeatable.
- D-04: The headless target should use minimal required dependencies only: CMake, C/C++ toolchain, pthreads, libconfuse, libftdi/libusb stack, and test tools only when tests are enabled.
- D-05: Prefer libftdi for Linux v1. Keep ftd2xx only if it costs almost nothing and does not complicate modern Linux builds.
- D-06: If Qt or TelldusCenter leaks into the `telldus-core` headless build, remove that accidental coupling in Phase 1.
- D-07: Use guards for Windows/macOS/FreeBSD paths if they interfere with modern Linux cleanup. Do not modernize or delete non-Linux logic in Phase 1.
- D-08: Prefer the least invasive CMake fix that unblocks modern Linux. Avoid broad global CMake baseline changes unless they are necessary to define the Linux headless boundary.
- D-12: If the build cannot configure without deeper modernization, fix only blockers directly tied to defining the headless boundary.

<threat_model>
## Threat Model

No new network or privilege surface is introduced. The relevant risks are build-chain integrity and accidental scope expansion:
- **T-01:** Build changes accidentally remove or modernize non-Linux behavior beyond Phase 1.
- **T-02:** Build changes hide missing dependencies instead of making headless requirements clear.
- **T-03:** Build changes break the public C API target name used by `tdtool`.
- **Mitigation:** Use guards only for non-Linux paths, keep Linux target names stable, and verify configure/dry-build target graph.
</threat_model>

<tasks>
  <task id="01-02-01" type="execute">
    <name>Make component toggles match the headless boundary</name>
    <title>Make component toggles match the headless boundary</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-CONTEXT.md
      - .planning/phases/01-headless-build-boundary/01-RESEARCH.md
      - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      - telldus-core/CMakeLists.txt
    </read_first>
    <action>
      Inspect `telldus-core/CMakeLists.txt`. Ensure the existing cache options can express the Phase 1 proof command: `BUILD_TDTOOL=TRUE`, `BUILD_TDADMIN=FALSE`, `ENABLE_TESTING=FALSE`, `FTDI_ENGINE=libftdi`, and `FORCE_COMPILE_FROM_TRUNK=TRUE`. If `BUILD_LIBTELLDUS-CORE` remains declared, make its behavior unambiguous: either honor it around `ADD_SUBDIRECTORY(client)` without breaking service/tdtool dependencies, or document in the boundary doc why it is not used for Phase 1. Add a new explicit option only if these existing options cannot make the headless boundary clear.
    </action>
    <verify>
      rg -n "BUILD_LIBTELLDUS-CORE|BUILD_TDTOOL|BUILD_TDADMIN|ENABLE_TESTING|ADD_SUBDIRECTORY\\(client|ADD_SUBDIRECTORY\\(tdtool|ADD_SUBDIRECTORY\\(tdadmin" telldus-core/CMakeLists.txt .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
    </verify>
    <acceptance_criteria>
      - `telldus-core/CMakeLists.txt` and/or `01-BUILD-BOUNDARY.md` makes the status of `BUILD_LIBTELLDUS-CORE` explicit.
      - `telldus-core/CMakeLists.txt` still contains `BUILD_TDTOOL` and `BUILD_TDADMIN`.
      - No repo-root build entry point is added.
    </acceptance_criteria>
  </task>

  <task id="01-02-02" type="execute">
    <name>Remove accidental Linux signing and GUI coupling blockers</name>
    <title>Remove accidental Linux signing and GUI coupling blockers</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-CONTEXT.md
      - telldus-core/cmake/FindSignTool.cmake
      - telldus-core/service/CMakeLists.txt
      - telldus-core/client/CMakeLists.txt
      - telldus-core/tdtool/CMakeLists.txt
    </read_first>
    <action>
      In `telldus-core/service/CMakeLists.txt` and `telldus-core/client/CMakeLists.txt`, keep Windows signing behavior but ensure Linux headless configure does not require signing tools. Replace unconditional `FIND_PACKAGE(SignTool REQUIRED)` usage with a guarded or non-required pattern that still defines `SIGN()` before `SIGN(...)` calls. Confirm no Qt or `telldus-gui` dependency is introduced or required by the `telldus-core/` build.
    </action>
    <verify>
      rg -n "FIND_PACKAGE\\( *SignTool|SIGN\\(" telldus-core/service/CMakeLists.txt telldus-core/client/CMakeLists.txt telldus-core/tdtool/CMakeLists.txt telldus-core/cmake/FindSignTool.cmake
      ! rg -n "Qt|TelldusCenter|telldus-gui" telldus-core/CMakeLists.txt telldus-core/service/CMakeLists.txt telldus-core/client/CMakeLists.txt telldus-core/tdtool/CMakeLists.txt
    </verify>
    <acceptance_criteria>
      - Linux configure path does not require a Windows signing executable or certificate.
      - `SIGN(` calls remain valid for existing targets.
      - `rg -n "Qt|TelldusCenter|telldus-gui" telldus-core/CMakeLists.txt telldus-core/service/CMakeLists.txt telldus-core/client/CMakeLists.txt telldus-core/tdtool/CMakeLists.txt` returns no matches.
    </acceptance_criteria>
  </task>

  <task id="01-02-03" type="execute">
    <name>Stabilize target graph for dry build proof</name>
    <title>Stabilize target graph for dry build proof</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-RESEARCH.md
      - telldus-core/service/CMakeLists.txt
      - telldus-core/client/CMakeLists.txt
      - telldus-core/tdtool/CMakeLists.txt
      - telldus-core/tests/CMakeLists.txt
      - telldus-core/tests/service/CMakeLists.txt
    </read_first>
    <action>
      Ensure the Linux target graph can be addressed by target names `telldusd`, `telldus-core`, and `tdtool`. If `tdtool` linking to `${CMAKE_BINARY_DIR}/client/libtelldus-core.so` blocks target graph clarity, change it to link through the existing target variable `${telldus-core_TARGET}` or a stable target reference while preserving Linux target output. Verify `ENABLE_TESTING=TRUE` still creates or expects `TelldusServiceStatic` only within the test boundary; do not fix full test execution unless target graph configure fails because of it.
    </action>
    <verify>
      rg -n "TARGET_LINK_LIBRARIES\\(tdtool|telldus-core_TARGET|TelldusServiceStatic|ENABLE_TESTING" telldus-core/tdtool/CMakeLists.txt telldus-core/service/CMakeLists.txt telldus-core/tests/CMakeLists.txt telldus-core/tests/service/CMakeLists.txt
    </verify>
    <acceptance_criteria>
      - The planned/proven target names include `telldusd`, `telldus-core`, and `tdtool`.
      - `tdtool` has an explicit dependency on the client library target via `ADD_DEPENDENCIES(tdtool ${telldus-core_TARGET})`.
      - Any test-boundary changes remain behind `ENABLE_TESTING`.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run when `cmake` is available:
- `cmake -S telldus-core -B build/telldus-core-headless -DFORCE_COMPILE_FROM_TRUNK=TRUE -DBUILD_TDTOOL=TRUE -DBUILD_TDADMIN=FALSE -DENABLE_TESTING=FALSE -DFTDI_ENGINE=libftdi`
- `cmake --build build/telldus-core-headless --target telldusd telldus-core tdtool -- -n`

If `cmake` is unavailable, record the exact `cmake: command not found` blocker in `01-BUILD-BOUNDARY.md` and leave source assertions green.
</verification>

<must_haves>
  <truths>
    - D-01: Keep `telldus-core/` as the build root.
    - D-03: Add an explicit CMake option/profile only if existing options are too unclear.
    - D-04: Keep minimal headless dependencies only.
    - D-05: Prefer libftdi; keep ftd2xx only if harmless.
    - D-06: Remove accidental Qt/TelldusCenter coupling if found.
    - D-07: Guard non-Linux paths; do not modernize or delete them.
    - D-08: Use the least invasive CMake fix that unblocks modern Linux.
    - D-12: Fix only blockers directly tied to defining the headless boundary.
  </truths>
</must_haves>

<success_criteria>
- Linux headless configure path avoids TelldusCenter and Qt.
- Signing is not a Linux configure blocker.
- Target graph for `telldusd`, `telldus-core`, and `tdtool` is clear enough for dry build proof.
</success_criteria>
