---
phase: 1
plan: 01-03
title: Prove and Document Headless Boundary
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
  - telldus-core/README
autonomous: true
requirements: [NBLD-01]
requirements_addressed: [NBLD-01]
must_haves:
  truths:
    - "D-01: Continue using `telldus-core/` as the build root."
    - "D-06: The proof must not require TelldusCenter or Qt."
    - "D-10: Phase 1 needs a dry build proof, not documentation alone."
    - "D-11: Use local Arch first; generic Linux container proof only if straightforward."
    - "D-12: Fix only blockers directly tied to defining the boundary."
    - "D-13: Commit exact headless configure/build instructions."
  artifacts:
    - path: .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      provides: "Dry build proof or precise environment blocker."
    - path: telldus-core/README
      provides: "Reproducible Linux headless configure/build instructions."
  key_links: []
---

<objective>
Produce the Phase 1 dry build proof or a precise environment blocker, and commit reproducible headless build instructions for Phase 2.
</objective>

## Decision Coverage

- D-01: Keep `telldus-core/` as the build root for Phase 1. Do not add a repo-root build entry point in this phase.
- D-06: If Qt or TelldusCenter leaks into the `telldus-core` headless build, remove that accidental coupling in Phase 1.
- D-10: Phase 1 is not complete with documentation alone. It should produce a dry build proof: configure plus at least enough target graph/build start to prove dependency and target selection.
- D-11: Use local Arch Linux first for the proof. Add a generic Linux container proof only if straightforward.
- D-12: If the build cannot configure without deeper modernization, fix only blockers directly tied to defining the headless boundary.
- D-13: Produce committed docs/build instructions with exact headless configure/build commands so Phase 2 can reproduce the boundary without guessing.

<threat_model>
## Threat Model

No runtime security behavior is introduced. The main operational risk is false proof:
- **T-01:** Documentation claims the headless boundary works without a reproducible command.
- **T-02:** A missing local tool is confused with a source-code failure.
- **Mitigation:** Record exact commands, exact outputs/blockers, and exact dependency prerequisites.
</threat_model>

<tasks>
  <task id="01-03-01" type="execute">
    <name>Run or record the headless configure proof</name>
    <title>Run or record the headless configure proof</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-VALIDATION.md
      - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      - telldus-core/CMakeLists.txt
    </read_first>
    <action>
      Run the configure command from `01-VALIDATION.md` if `cmake` is available. If `cmake` is still unavailable, do not install packages unless explicitly approved; update `01-BUILD-BOUNDARY.md` with a `Proof Result` subsection that records `cmake: command not found` as an environment prerequisite blocker, not a source failure. If configure runs, record whether it succeeds and name any missing dependency reported by CMake.
    </action>
    <verify>
      rg -n "Proof Result|cmake -S telldus-core|FORCE_COMPILE_FROM_TRUNK|BUILD_TDADMIN=FALSE|FTDI_ENGINE=libftdi|cmake: command not found|Configure" .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
    </verify>
    <acceptance_criteria>
      - `01-BUILD-BOUNDARY.md` contains heading `## Proof Result`.
      - `01-BUILD-BOUNDARY.md` contains `cmake -S telldus-core`.
      - `01-BUILD-BOUNDARY.md` records either configure success or the exact first blocker.
    </acceptance_criteria>
  </task>

  <task id="01-03-02" type="execute">
    <name>Run or record the dry build target proof</name>
    <title>Run or record the dry build target proof</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-VALIDATION.md
      - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      - telldus-core/service/CMakeLists.txt
      - telldus-core/client/CMakeLists.txt
      - telldus-core/tdtool/CMakeLists.txt
    </read_first>
    <action>
      If configure succeeded, run the dry build command for targets `telldusd`, `telldus-core`, and `tdtool`. If the generated backend does not support `-- -n`, use the closest target-list or build-start proof available and record the substitution. Update `01-BUILD-BOUNDARY.md` with target proof output or the exact blocker.
    </action>
    <verify>
      rg -n "Dry Build Proof|telldusd|telldus-core|tdtool|target" .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
    </verify>
    <acceptance_criteria>
      - `01-BUILD-BOUNDARY.md` contains heading `## Dry Build Proof`.
      - `01-BUILD-BOUNDARY.md` contains `telldusd`, `telldus-core`, and `tdtool`.
      - `01-BUILD-BOUNDARY.md` records either dry build proof success or the exact blocker preventing it.
    </acceptance_criteria>
  </task>

  <task id="01-03-03" type="execute">
    <name>Commit reproducible headless build instructions</name>
    <title>Commit reproducible headless build instructions</title>
    <read_first>
      - .planning/phases/01-headless-build-boundary/01-CONTEXT.md
      - .planning/phases/01-headless-build-boundary/01-RESEARCH.md
      - .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md
      - telldus-core/README
      - telldus-core/INSTALL
    </read_first>
    <action>
      Update `telldus-core/README` with a concise `HEADLESS LINUX BUILD` section. Include allowed scope, excluded GUI scope, prerequisites, the exact configure command, the exact dry build command, and a note that full Arch build/test fixing continues in Phase 2. Do not document Docker runtime or Raspberry Pi-specific steps here beyond saying they are later phases.
    </action>
    <verify>
      rg -n "HEADLESS LINUX BUILD|telldusd|telldus-core|tdtool|FORCE_COMPILE_FROM_TRUNK|BUILD_TDADMIN=FALSE|FTDI_ENGINE=libftdi|TelldusCenter|Phase 2" telldus-core/README
    </verify>
    <acceptance_criteria>
      - `telldus-core/README` contains heading `HEADLESS LINUX BUILD`.
      - `telldus-core/README` contains `telldusd`, `telldus-core`, `tdtool`, `FORCE_COMPILE_FROM_TRUNK`, `BUILD_TDADMIN=FALSE`, and `FTDI_ENGINE=libftdi`.
      - `telldus-core/README` states TelldusCenter/Qt GUI is outside the headless build.
      - `telldus-core/README` states full Arch build/test fixing continues in Phase 2.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `rg -n "Proof Result|Dry Build Proof" .planning/phases/01-headless-build-boundary/01-BUILD-BOUNDARY.md`
- `rg -n "HEADLESS LINUX BUILD|FORCE_COMPILE_FROM_TRUNK|FTDI_ENGINE=libftdi" telldus-core/README`
- If `cmake` is available: run the configure and dry build commands from `01-VALIDATION.md`.
</verification>

<must_haves>
  <truths>
    - D-10: Phase 1 needs a dry build proof, not documentation alone.
    - D-11: Use local Arch first; generic Linux container proof only if straightforward.
    - D-12: Fix only blockers directly tied to defining the boundary.
    - D-13: Commit exact headless configure/build instructions.
    - D-01: Continue using `telldus-core/` as the build root.
    - D-06: The proof must not require TelldusCenter or Qt.
  </truths>
</must_haves>

<success_criteria>
- Phase 2 can reproduce the exact headless configure and dry build commands.
- Any inability to complete the dry build proof is recorded as an exact environment or source blocker.
- `telldus-core/README` contains committed headless Linux build instructions.
</success_criteria>
