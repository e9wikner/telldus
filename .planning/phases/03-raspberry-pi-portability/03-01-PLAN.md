---
phase: 03-raspberry-pi-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-debian-deps.sh
  - docs/phase-03-docker-commands.md
autonomous: true
requirements:
  - NBLD-03
user_setup:
  - service: docker
    why: "Docker multi-arch build requires docker buildx and QEMU binfmt for linux/arm64 emulation"
    env_vars: []
    dashboard_config: []
    manual_steps:
      - "Ensure Docker Engine is installed and the docker daemon is running"
      - "Ensure docker buildx is available: docker buildx inspect default"
      - "Ensure QEMU binfmt is configured for aarch64: docker run --rm --privileged multiarch/qemu-user-static --reset -p yes"

must_haves:
  truths:
    - "Developer can install all Debian build dependencies with a single script"
    - "Debian package names are verified against the actual bookworm-slim package index"
    - "Documented Docker multi-arch build commands reproduce the full build-test-smoke pipeline"
  artifacts:
    - path: "scripts/install-debian-deps.sh"
      provides: "One-command Debian/Raspberry Pi OS dependency installer"
      contains: "apt-get install -y -q cmake build-essential pkg-config libftdi1-dev libconfuse-dev libusb-1.0-0-dev libcppunit-dev"
    - path: "docs/phase-03-docker-commands.md"
      provides: "Verified Docker multi-arch build command reference"
      contains: "docker run --rm --platform linux/arm64"
  key_links:
    - from: "scripts/install-debian-deps.sh"
      to: "debian:bookworm-slim apt index"
      via: "apt-get install"
      pattern: "apt-get install -y -q .*libftdi1-dev"
    - from: "docs/phase-03-docker-commands.md"
      to: "CMakePresets.json headless preset"
      via: "cmake -B build/headless"
      pattern: "FORCE_COMPILE_FROM_TRUNK=TRUE.*FTDI_ENGINE=libftdi"
---

<objective>
Create the Debian/Raspberry Pi OS `aarch64` build environment by producing a dependency installation script and documenting the exact Docker multi-arch build commands.

Purpose: Establishes the reproducible Debian build surface and command reference that Wave 2 will execute.
Output: `scripts/install-debian-deps.sh` and `docs/phase-03-docker-commands.md`.
</objective>

<execution_context>
@/home/rsw/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/rsw/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-raspberry-pi-portability/03-CONTEXT.md
@.planning/phases/03-raspberry-pi-portability/03-RESEARCH.md
@.planning/phases/02-arch-native-build/02-01-SUMMARY.md
@scripts/install-arch-deps.sh
@CMakePresets.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/install-debian-deps.sh</name>
  <files>scripts/install-debian-deps.sh</files>
  <read_first>
    - scripts/install-arch-deps.sh (pattern to mirror: shebang, set -e, header echo, package list, error fallback)
    - .planning/phases/03-raspberry-pi-portability/03-RESEARCH.md (exact package list per D-03-05: cmake, build-essential, libftdi1-dev, libconfuse-dev, libusb-1.0-0-dev, pkg-config, libcppunit-dev)
  </read_first>
  <action>
    Create scripts/install-debian-deps.sh per D-03-04 and D-03-05, mirroring the Arch script structure:
    - Start with #!/bin/bash and set -e
    - Export DEBIAN_FRONTEND=noninteractive to suppress debconf warnings in non-interactive environments
    - Echo a header: "Installing Telldus Core headless build dependencies on Debian/Raspberry Pi OS..."
    - Run apt-get update -qq
    - Run apt-get install -y -q with the exact package list: cmake build-essential pkg-config libftdi1-dev libconfuse-dev libusb-1.0-0-dev libcppunit-dev
    - Add an error fallback block that prints a warning and exits 1 on failure
    - End with "All dependencies installed successfully."
    Do NOT use libftdi-dev (legacy 0.x); use libftdi1-dev (1.x) per RESEARCH.md pitfall 1.
    Do NOT include apt instead of apt-get; apt-get is the stable scripting interface.
  </action>
  <acceptance_criteria>
    - scripts/install-debian-deps.sh exists and is executable (chmod +x)
    - File contains #!/bin/bash
    - File contains set -e
    - File contains DEBIAN_FRONTEND=noninteractive
    - File contains apt-get install -y -q cmake build-essential pkg-config libftdi1-dev libconfuse-dev libusb-1.0-0-dev libcppunit-dev
    - File does NOT contain libftdi-dev (without the '1')
  </acceptance_criteria>
  <verify>
    <automated>
      test -x scripts/install-debian-deps.sh && \
      grep -q '#!/bin/bash' scripts/install-debian-deps.sh && \
      grep -q 'set -e' scripts/install-debian-deps.sh && \
      grep -q 'DEBIAN_FRONTEND=noninteractive' scripts/install-debian-deps.sh && \
      grep -q 'libftdi1-dev' scripts/install-debian-deps.sh && \
      grep -q 'libconfuse-dev' scripts/install-debian-deps.sh && \
      ! grep -q 'libftdi-dev[^1]' scripts/install-debian-deps.sh && \
      echo "PASS"
    </automated>
  </verify>
  <done>Standalone Debian dependency script exists with exact verified package names, set -e, and DEBIAN_FRONTEND=noninteractive.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Debian package names in bookworm-slim container</name>
  <files>scripts/install-debian-deps.sh</files>
  <read_first>
    - scripts/install-debian-deps.sh (package list to verify)
    - .planning/phases/03-raspberry-pi-portability/03-RESEARCH.md (verification method: apt-cache search inside container)
  </read_first>
  <action>
    Verify every package in the script resolves in the debian:bookworm-slim package index.
    For each package in [cmake, build-essential, pkg-config, libftdi1-dev, libconfuse-dev, libusb-1.0-0-dev, libcppunit-dev]:
    - Run: docker run --rm debian:bookworm-slim bash -c "apt-get update -qq >/dev/null 2>&1 && apt-cache search --names-only '^PACKAGE$'"
    - Assert the output is non-empty.
    If any package returns empty, document the discrepancy and the replacement package name found via apt-cache search.
    Per D-03-05, the agent may adjust package names if apt-get reports different names.
  </action>
  <acceptance_criteria>
    - docker run for each package returns non-empty output
    - All 7 packages are confirmed present in the Debian Bookworm index
    - Any package name discrepancy is documented in the task output
  </acceptance_criteria>
  <verify>
    <automated>
      for pkg in cmake build-essential pkg-config libftdi1-dev libconfuse-dev libusb-1.0-0-dev libcppunit-dev; do
        out=$(docker run --rm debian:bookworm-slim bash -c "apt-get update -qq >/dev/null 2>&1 && apt-cache search --names-only '^'"${pkg}"'$'")
        if [ -z "$out" ]; then echo "FAIL: $pkg not found"; exit 1; fi
        echo "OK: $pkg"
      done
    </automated>
  </verify>
  <done>All 7 Debian package names verified against the live bookworm-slim package index; no unresolved packages remain.</done>
</task>

<task type="auto">
  <name>Task 3: Document Docker multi-arch build commands</name>
  <files>docs/phase-03-docker-commands.md</files>
  <read_first>
    - .planning/phases/03-raspberry-pi-portability/03-RESEARCH.md (verified Docker command patterns, anti-patterns: read-only mount, cache mismatch)
    - CMakePresets.json (headless preset cache variables)
    - .planning/phases/02-arch-native-build/02-01-SUMMARY.md (repo-root symlink pattern for preset usage)
  </read_first>
  <action>
    Create docs/phase-03-docker-commands.md documenting the exact verified Docker multi-arch build pipeline.
    The document must include:
    1. A prerequisites section listing Docker, buildx, and QEMU binfmt
    2. The exact docker run command for linux/arm64 using debian:bookworm-slim, including:
       - --rm --platform linux/arm64
       - -v $(pwd):/src:ro (read-only source mount)
       - DEBIAN_FRONTEND=noninteractive environment variable
       - apt-get update -qq && apt-get install -y -q with the exact 7 packages
       - cp -r /src /work (copy to writable directory per RESEARCH.md anti-pattern 1)
       - cd /work/telldus-core
       - cmake -B build/headless with cache variables: FORCE_COMPILE_FROM_TRUNK=TRUE, BUILD_TDTOOL=TRUE, BUILD_TDADMIN=FALSE, ENABLE_TESTING=TRUE, FTDI_ENGINE=libftdi
       - cmake --build build/headless --parallel $(nproc)
       - ctest --test-dir build/headless -R cppunit --output-on-failure
       - ./build/headless/tdtool/tdtool --help
    3. A note that the headless preset is reused unchanged per D-03-06
    4. A note about QEMU emulation being 5-10x slower and setting appropriate timeouts
    Do NOT include a Dockerfile; Phase 5 handles production container images.
  </action>
  <acceptance_criteria>
    - docs/phase-03-docker-commands.md exists
    - Document contains docker run --rm --platform linux/arm64
    - Document contains debian:bookworm-slim
    - Document contains cp -r /src /work
    - Document contains cmake -B build/headless
    - Document contains ctest --test-dir build/headless -R cppunit
    - Document contains ./build/headless/tdtool/tdtool --help
    - Document contains DEBIAN_FRONTEND=noninteractive
    - Document mentions QEMU emulation performance warning
    - Document does NOT contain a Dockerfile
  </acceptance_criteria>
  <verify>
    <automated>
      test -f docs/phase-03-docker-commands.md && \
      grep -q 'docker run --rm --platform linux/arm64' docs/phase-03-docker-commands.md && \
      grep -q 'debian:bookworm-slim' docs/phase-03-docker-commands.md && \
      grep -q 'cp -r /src /work' docs/phase-03-docker-commands.md && \
      grep -q 'cmake -B build/headless' docs/phase-03-docker-commands.md && \
      grep -q 'ctest --test-dir build/headless -R cppunit' docs/phase-03-docker-commands.md && \
      grep -q 'tdtool --help' docs/phase-03-docker-commands.md && \
      grep -q 'DEBIAN_FRONTEND=noninteractive' docs/phase-03-docker-commands.md && \
      grep -q 'QEMU' docs/phase-03-docker-commands.md && \
      ! grep -q 'FROM ' docs/phase-03-docker-commands.md && \
      echo "PASS"
    </automated>
  </verify>
  <done>Verified Docker multi-arch build command reference exists with complete build, test, and smoke sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker daemon | Host runs container with Debian image |
| Container → Debian apt repos | Container fetches packages from Debian repositories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | debian:bookworm-slim base image | mitigate | Pull from official Docker Hub; use docker pull debian:bookworm-slim@sha256:... if digest pinning is available; verify image provenance |
| T-03-01-02 | Tampering | Debian package names in script | mitigate | Verify each package with apt-cache search inside live debian:bookworm-slim container before committing the script; use official Debian repositories only |
</threat_model>

<verification>
- scripts/install-debian-deps.sh exists, is executable, and contains exact package list per D-03-05
- All 7 package names resolve in debian:bookworm-slim apt index
- docs/phase-03-docker-commands.md contains complete build-test-smoke command sequence
- No Dockerfile is committed (deferred to Phase 5)
</verification>

<success_criteria>
- Developer can run scripts/install-debian-deps.sh on a Debian/Raspberry Pi OS system to install all build dependencies
- Docker multi-arch build commands are documented and ready for Wave 2 execution
- Package names are verified against live Debian Bookworm index
</success_criteria>

<output>
After completion, create `.planning/phases/03-raspberry-pi-portability/03-01-SUMMARY.md`
</output>
